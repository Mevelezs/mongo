use('sample_training');


db.zips.updateMany(
  // query de consulta
  { city : 'CLEVELAND'},
  // update => operators
  {
    $rename : {
      prueva : 'prueba', // renombra el nombre no el valor del atributo
    }
  }
)
